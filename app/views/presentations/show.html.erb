<div class='page-header'>
	<h1><%= @presentation.title %></h1>
	<p>
		<% if current_user.admin? %>
			<%= link_to 'Edit', edit_presentation_path(@presentation) %> |
		<% end %>
		<%= link_to 'Download', @presentation.slides.url, :download=>@presentation.title+".pdf" %> | <%= link_to 'Return to list of presentations', presentations_path %>
	</p>
</div>

<p id="notice"><%= notice %></p>


<%= render 'discussion' %>