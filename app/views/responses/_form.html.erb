<% if !@response.nil? && @response.assignment.due_date< Time.zone.now %>
<h1> The due date has passed, you can no longer edit this response. </h1>
<h1><%=@response.title%></h1>
<h3>By: <%=@response.user.name%></h3>
<br>
<div class="responsecontent">
  <%=@response.content.html_safe%>
</div>

<% else %>

<h1><%=@assignment.discussion_question.html_safe%></h1> 

<%= form_for(@response) do |f| %>
  <% if @response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@response.errors.count, "error") %> prohibited this response from being saved:</h2>

      <ul>
      <% @response.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <% unless @presentation.nil? %> 
        <iframe src="<%= @presentation.slides.url %>#page=<%= @assignment.slide_number %>" frameborder="0" width="400" height="400" allowfullscreen="true" mozallowfullscreen="true" webkitallowfullscreen="true"></iframe>
    <% end %>

  <div class="field">
     <div>
    <%= f.hidden_field :assignment_id, value: @assignment.id%>
  </div>
  </div>

  <div class="field">
     <div>
    <%= f.label :content %><br>
    <%= f.cktext_area :content, :ckeditor => {:language => 'en'} %>
  </div>
  </div>

  <div class="field">
    <%= f.hidden_field :user_id, value: @user.id%>
  </div>

  <div class="field">
    <%= f.hidden_field :user_name, value: @user.name%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<% end %>